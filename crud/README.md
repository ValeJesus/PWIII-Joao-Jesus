# criação do CRUD com laravel

--

# Criação do projeto : 

´´´
composer create-project laravel/laravel (nome do projeto(nesse caso crud!))
´´´

Apos isso entre na pasta com :

```
cd (nome do projeto)
```

